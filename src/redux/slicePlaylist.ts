import { createSlice } from "@reduxjs/toolkit";

const INITIAL_STATE = {};

const slicePlaylist = createSlice({
  name: "playlist",
  initialState: INITIAL_STATE,
  reducers: {},
});

export default slicePlaylist;
